# Autonomous-AI-Agent
a program where an LLM (Llama) doesn't just answer questions, but decides which tools to use and takes multiple steps to solve a problem. Think of it like giving the AI a toolbox and letting it figure out which tool to grab.
